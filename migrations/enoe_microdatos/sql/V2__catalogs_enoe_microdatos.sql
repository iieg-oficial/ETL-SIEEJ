CREATE TABLE IF NOT EXISTS cat_enoe_sector (
    id          INTEGER PRIMARY KEY,
    descripcion TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS cat_enoe_ocupacion (
    id          INTEGER PRIMARY KEY,
    descripcion TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS cat_enoe_situacion_trabajo (
    id          INTEGER PRIMARY KEY,
    descripcion TEXT NOT NULL
);
